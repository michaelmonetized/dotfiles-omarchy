[ids]
*

[main]
# Map the fn key to activate the hyper layer
fn = layer(hyper_layer)

[hyper_layer]
# This tells keyd to hold ctrl+meta+alt+shift when fn is held down
# Note: Linux registers 'meta' as the Super/Command key
fn = chord(control, meta, alt, shift)
